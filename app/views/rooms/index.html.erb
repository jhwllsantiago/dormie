<div class="container-fluid mh mb-custom py-3 bg-repeat">
  <h3 class="text-center monsterrat heading-color semiBold p-3" >Rooms for Rent</h3>
  <div class="d-flex flex-wrap justify-content-center">
    <% @rooms.each do |room| %>
      <%= render "room", room: room %>
    <% end %>
  </div>
</div>
